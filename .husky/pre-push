#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🚀 Running pre-push checks (this may take a moment)..."
echo ""

# Check if zensical is installed
if ! command -v zensical &> /dev/null; then
  echo "⚠️  Zensical not found, skipping build check"
  echo "   Install with: pip install zensical"
else
  # Build documentation
  echo "🏗️  Building documentation..."
  zensical build --quiet 2>/dev/null || zensical build

  if [ $? -ne 0 ]; then
    echo ""
    echo "❌ Documentation build failed!"
    echo "   Fix the errors above before pushing."
    echo ""
    exit 1
  fi

  echo "✅ Documentation builds successfully"
fi

echo ""
echo "🔗 Running comprehensive link check on all documentation..."

# Check if markdown-link-check is available
if command -v markdown-link-check &> /dev/null; then
  # Find all markdown files in docs directory
  MARKDOWN_FILES=$(find docs -name "*.md" 2>/dev/null || echo "")

  if [ -n "$MARKDOWN_FILES" ]; then
    LINK_ERRORS=0

    for file in $MARKDOWN_FILES; do
      echo "   Checking: $file"
      markdown-link-check "$file" --quiet 2>&1 | grep -E "(ERROR|✖)" && LINK_ERRORS=$((LINK_ERRORS + 1)) || true
    done

    if [ $LINK_ERRORS -gt 0 ]; then
      echo ""
      echo "⚠️  Found broken links in $LINK_ERRORS file(s)"
      echo "   This is a warning - you can still push, but please fix broken links"
      echo ""
    else
      echo "✅ All links are valid"
    fi
  fi
else
  echo "⚠️  markdown-link-check not found, skipping link validation"
  echo "   Install with: npm install"
fi

echo ""
echo "✅ Pre-push checks completed!"
echo "📤 Proceeding with push..."
